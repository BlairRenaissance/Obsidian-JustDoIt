
欧拉旋转是一种动态轴旋转，是 **基于自身坐标系** 的。相比于传统的固定轴旋转有很大区别。

这符合真实物体（如飞机、机械臂）的旋转。飞机俯仰（Pitch）时，机头始终绕 **机身自身的 Y 轴** 旋转，而非地面坐标系的 Y 轴。

## Euler Angles 欧拉角

### 欧拉角简介

Proper Euler angles（固有欧拉角）和 Tait - Bryan angles（泰特 - 布莱恩角）是两种欧拉旋转的方式。

Proper Euler angles ：z-x-z, x-y-x, y-z-y, z-y-z, x-z-x, y-x-y
Tait–Bryan angles ：x-y-z, y-z-x, z-x-y, x-z-y, z-y-x, y-x-z

虽然固有欧拉角的方式旋转（仅绕两个轴旋转），从数学计算上来说是完全能够实现三维旋转的，但更容易出现奇点。

因此，实际操作中更常用的是泰特 - 布莱恩角，即绕三个轴旋转（在 Blender 等软件中仍然称其为Euler，比如 XYZ Euler / YZX Euler 等等）。

我们常说的偏航（yaw）/ 俯仰（pitch）/ 滚转（roll）指的也是泰特 - 布莱恩角中的操作。

![](https://raw.githubusercontent.com/BlairRenaissance/ImageHost/main/20250710113939.png)

---
### 旋转顺序

在三维空间里，我们有三个基本坐标轴：X轴、Y轴和Z轴。每一次旋转都可以选择绕这三个轴中的某一个进行。从排列组合的角度来看，总共有6种旋转顺序。

已知右手定则，且旋转矩阵表达的是XYZ轴旋转之后的位置，则很容易得到：
![500](https://raw.githubusercontent.com/BlairRenaissance/ImageHost/main/Clipboard_Screenshot_1752119490.png)

设旋转顺序为：

1. 先绕 **X轴** 旋转角度 `α` → 矩阵 `R_x(α)`
    
2. 再绕 **新Y轴** 旋转角度 `β` → 矩阵 `R_y(β)`
    
3. 最后绕 **新Z轴** 旋转角度 `γ` → 矩阵 `R_z(γ)`

则最终旋转矩阵为：  
$$Rfinal=Rz(γ)⋅Ry(β)⋅Rx(α)$$

---
### 内旋外旋

> [!Abustruct]
> 外旋(extrinsic rotations) = 固定轴 = 左乘(R矩阵在左)。
> 内旋 (intrinsic rotations) = 旋转轴 = 右乘(R矩阵在右)。

![600](https://raw.githubusercontent.com/BlairRenaissance/ImageHost/main/Clipboard_Screenshot_1752129140.png)

自己计算(1,0,0)点先X转90°再Y转90°时，固定系结果是(0,0,-1)，而动态系是(0,1,0)。`

> [!Important]
> 关键理解：内旋的右乘本质是坐标系的递归变换
> 

1. 初始状态：世界坐标系W，物体坐标系B与W重合

2. 第一次绕B-X转：物体动，B系相对于W系旋转（用矩阵$R_x$描述）

3. 而后要在新B系中绕Y轴转

4. 但旋转矩阵$R_y$是定义在原始B系（即W系）中的，所以需要先转回原始B系，应用旋转矩阵$R_y$，再还原回新B系
$$ 
v_2 = [R_X \cdot R_Y \cdot R_X^{-1}] \cdot v_1 
= \underbrace{R_X \cdot R_Y \cdot \cancel{R_X^{-1}} \cdot R_X}_{\text{递归嵌入}} \cdot v_0 
= \underbrace{R_X \cdot R_Y}_{\text{右乘！}} \cdot v_0 
$$

| **旋转类型**          | 旋转轴定义       | 矩阵叠加方向 | 乘法顺序            | 数学意义           |
| ----------------- | ----------- | ------ | --------------- | -------------- |
| **外旋(Extrinsic)** | 固定于空间坐标系(W) | 左乘     | `R_new · R_old` | 新变换作用于原始坐标系    |
| **内旋(Intrinsic)** | 固连于物体坐标系(B) | 右乘     | `R_old · R_new` | 新变换作用于被修改后的坐标系 |

---
## Gimbal 万向节

**万向节**是一种枢轴支架，允许物体绕一个轴旋转（一个环就是一个万向节）。

由三个万向节组成的装置（外环套在内环上，且枢轴彼此正交）可用于使安装在最内层万向节上的物体保持独立于其支撑物的旋转，例如保持垂直。

> [!Tip]
> 万向节就是内环先转，外环套内环，所以外环的转动也会作用在内环上。

在船舶上，陀螺仪、船用罗盘、炉灶，甚至饮料架通常都使用万向节，以便船体在上下左右摇晃时，它们仍能相对于地平线保持直立。所以也可以用来制造云台～
![](https://raw.githubusercontent.com/BlairRenaissance/ImageHost/main/Gyroscope_operation.gif)

为什么有的轴会跟随别的轴旋转？
- **物理约束**：如下图中，外环自身的转动也会作用于中环与内环。（上图中启用了逆向思维，即不希望中心物体转时外环该怎么转）。
- **数学约束**：旋转矩阵乘法中，后乘的旋转矩阵作用于被前序旋转修改后的坐标系。

显然三层叠加的万向节装置，是三维动态轴旋转的完美物理表达。

![](https://raw.githubusercontent.com/BlairRenaissance/ImageHost/main/Gimbal_3_axes_rotation.gif)

---
## Gimbal Lock 万向节锁🔒

假设X轴红色向右，Y轴绿色向外，Z轴蓝色向上。

沿Y轴转90度时，红蓝的转动是反向抵抗的关系；沿Y轴转270度时，红蓝的转动是同向叠加的关系（如下图所示）。

![300](https://raw.githubusercontent.com/BlairRenaissance/ImageHost/main/Clipboard_Screenshot_1752133034.png)

![300](https://raw.githubusercontent.com/BlairRenaissance/ImageHost/main/Clipboard_Screenshot_1752133107.png)

这就是万向节锁。

从数学角度证明一下：

上图中的旋转顺序是X-Y-Z（万向节就是内环先转，外环套内环）。

从上一章中我们知道，动态轴旋转的计算是右乘旋转矩阵，即哪个轴最后转先乘哪个轴。
$$R=R_Z(γ)⋅R_Y​(β)⋅R_X​(α)$$

当 Y 轴旋转角度$\beta = 270^\circ$（即$\beta = \frac{3\pi}{2}$）时：
$$
R = 
\begin{bmatrix}
\cos\gamma & -\sin\gamma & 0 \\
\sin\gamma & \cos\gamma & 0 \\
0 & 0 & 1
\end{bmatrix}
\cdot
\begin{bmatrix}
0 & -\sin\alpha & -\cos\alpha \\
0 & \cos\alpha & -\sin\alpha \\
1 & 0 & 0
\end{bmatrix}
$$
$$
=
\begin{bmatrix}
0 & -\cos\gamma\sin\alpha - \sin\gamma\cos\alpha & -\cos\gamma\cos\alpha + \sin\gamma\sin\alpha \\
0 & -\sin\gamma\sin\alpha + \cos\gamma\cos\alpha & -\sin\gamma\cos\alpha - \cos\gamma\sin\alpha \\
1 & 0 & 0
\end{bmatrix}
$$

利用三角函数和角公式：
$$
\sin(\alpha + \gamma) = \sin\alpha\cos\gamma + \cos\alpha\sin\gamma
$$
$$
\cos(\alpha + \gamma) = \cos\alpha\cos\gamma - \sin\alpha\sin\gamma
$$
简化后的矩阵为：

$$
R = 
\begin{bmatrix}
0 & -\sin(\alpha + \gamma) & -\cos(\alpha + \gamma) \\
0 & \cos(\alpha + \gamma) & -\sin(\alpha + \gamma) \\
1 & 0 & 0
\end{bmatrix}
$$
哈！出现了！叠加态！

同样可证当 Y 轴旋转角度$\beta = 90^\circ$（即$\beta = \frac{1\pi}{2}$）时，哈！出现了！抵抗态！

$$
R = 
\begin{bmatrix}
0 & -\sin(\gamma - \alpha) & \cos(\gamma - \alpha) \\
0 & \cos(\gamma - \alpha) & \sin(\gamma - \alpha) \\
-1 & 0 & 0
\end{bmatrix}
$$

---

因为万向锁的存在，要绕三个轴进行旋转时，我们往往不用欧拉旋转。主要是在绕两个轴旋转的时候会用。

选择旋转矩阵时，最左侧的是最主要的旋转轴（大喊右乘！），中间的是我们不会用到的轴。

![](https://raw.githubusercontent.com/BlairRenaissance/ImageHost/main/Clipboard_Screenshot_1752136137.png)


那如果我们想绕三个轴进行旋转呢？嘿👋！四元数。

[[Quaternion 四元数]]