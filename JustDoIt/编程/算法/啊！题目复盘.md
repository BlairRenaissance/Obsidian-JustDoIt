
### LC 1. 两数之和

1. 核心套路

• 归类：双指针 / 哈希表 / DFS / 动态规划...
• 思路：一句话描述解法（比如：用哈希表记录即使数值，遍历查找 target - num）

2. 复杂度

• 时间：O(N)
• 空间：O(N)

3. C++ 易错点 (关键！)

• 比如：`std::map` 找不到key会默认插入0，要用 `.find()`
• 比如：`vector` 下标越界问题
• 比如：`int` 溢出，需要用 `long long`
  
4. 核心代码片段

```

// 记录最关键的 3-5 行逻辑，不要贴全代码

```